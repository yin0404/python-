6.10
py语言左开右闭
type：用于对变量类型的检查
int：整数，不区分长整数
float：浮点数、小数，占位符后.x，x即为几位小数
str：字符串，注意不可以和数字一起进行计算，转变i，f
chr：将整数转换成该编码对应的字符串（一个字符）。
ord：将字符串（一个字符）转换成对应的编码（整数）
bool：Ture or False，注意and与or可以连续进行逻辑运算。
          运算符	描述
[] [:]	             下标，切片
**	                 指数
~ + -	               按位取反, 正负号
* / % //	           乘，除，模，整除
+ -	                 加，减
>> <<	               右移，左移
&	                   按位与
^ |	                 按位异或，按位或
<= < > >=	           小于等于，小于，大于，大于等于
== !=	               等于，不等于
is is not	           身份运算符（ID运算）
in not in	           成员运算符
not or and	         逻辑运算符
= += -= *= /= %= //= **= &= 	      = ^= >>= <<=
多分支情况下尽量使用if elif else，不用嵌套
6.11
一：range：构造一个数字范围，可以在第三位设定步长：
range(1, 101, 2)：可以用来产生1到100的奇数，其中2是步长，即每次数值递增的值。
range(100, 0, -2)：可以用来产生100到1的偶数，其中-2是步长，即每次数字递减的值。
二：len-内置函数，用于计算字符长度
三：通用于所有类型称为function，对应单个类型，比如字符串、数型的，称为method
四：round-内置函数，四舍五入
五：abs-内置函数，绝对值
6.12：
一：带默认值的参数必须放在不带默认值的参数之后，否则将产生SyntaxError错误，
错误消息是：non-default argument follows default argument，翻译成中文的意思是“没有默认值的参数放在了带默认值的参数后面”。
